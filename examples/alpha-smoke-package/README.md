# Alpha Smoke Package

This package exists only to prove the current StoryShell package shape for Linux tester smoke tests.

It is not a finished game and it does not imply a runtime engine beyond the current scaffold.
