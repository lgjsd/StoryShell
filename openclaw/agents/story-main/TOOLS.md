# TOOLS.md

Use the installed manifest and wrappers when present.

Expected wrappers:
- `storyshell-manifest`
- `storyshell-validate`
- `storyshell-state`
- `storyshell-python`

Do not rediscover repo paths with recursive search unless the manifest is missing.
